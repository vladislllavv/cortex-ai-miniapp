meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  installCommand: npm install
  buildCommand: npm run build

run:
  # Вот тут мы говорим Amvera запустить файл бота
  scriptName: node bot.js 
